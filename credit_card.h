// Contains declarations of classes, functions and variables for functionality
#include "stdc++.h"
#include <string>
#include <cctype>

bool basicFormatingCheck(std::string card_number){};
void creditCardValidation(std::string card_number);
std::string identifyCardType(const std::string& card_number);
